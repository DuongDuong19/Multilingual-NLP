# ================================================================
# Multilingual NLP: mmBERT-small + NER + mT5-small
# ================================================================
torch>=2.1.0
transformers>=4.44.0      # cần >= 4.44 để support ModernBERT (mmBERT)
tokenizers>=0.19.0
datasets>=2.18.0
accelerate>=0.27.0        # cho mixed precision & multi-GPU
sentencepiece>=0.1.99     # mT5 tokenizer
seqeval>=1.2.2            # metrics cho NER (F1, precision, recall)
numpy>=1.24.0
tqdm>=4.66.0
scipy>=1.11.0
scikit-learn>=1.3.0
pyyaml>=6.0               # load config từ YAML
tensorboard>=2.14.0       # logging (optional)